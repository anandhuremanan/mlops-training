name: Greeting Action

on:
  pull_request:
    types: [closed]

jobs:
  greet:
    if: github.event.pull_request.merged == true && github.event.pull_request.base.ref == 'dev'
    runs-on: ubuntu-latest
    steps:
      - name: Say Hello
        run: echo "🎉 PR merged into dev! Congrats!"
